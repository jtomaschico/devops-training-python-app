pipeline {
  // `agent any` = el pipeline se ejecuta en cualquier nodo/agente disponible.
  agent any

  environment {
    PYTHON_TEST_IMAGE = 'python:3.6-slim'
  }  

  agent {
    docker { 
        image PYTHON_TEST_IMAGE
        args '-u root' 
    }
  }

  stages {
    stage('Info') {
      steps {
        echo "Job: ${env.JOB_NAME}"
        echo "Build: ${env.BUILD_NUMBER}"
      }
    }
    stage('Install') {
       steps {
          sh 'apt-get update && apt-get install -y make'
          sh 'pip install -r requirements.txt'
       }
    }
    stage('CI - Lint') {
      steps {
        sh 'apt-get update && apt-get install -y make'
        sh 'make lint'
      }
    } 
    stage('CI - Test') {
      steps {
         h 'pytest -q'
      }
    }
  }
} 
